@using WebApp_TransportCompany.ViewModels
@model OrderFormPartialViewModel
@{
    ViewData["Title"] = "Edit";
}

<div class="card">
    <div class="card-header">
        <h1>Изменение</h1>
    </div>
    <div class="card-body">
        @{ 
            await Html.RenderPartialAsync("_OrderFormPartial", Model);
        }
    </div>
</div>