<script>
    $(function () {
        //$('a[data-toggle="wheel-details-modal"]').click(function (event) {
        //    var url = "/Wheel/Details/" + $(this).attr('item');
        //    $.get(url).done(function (data) {
        //        $('#wheel-details-modal-placeholder').html(data);
        //        $('#wheel-details-modal-placeholder > .modal').modal('show');
        //    });
        //});

        //$('a[data-toggle="wheel-edit-modal"]').click(function (event) {
        //    var url = "/Wheel/Edit/" + $(this).attr('item');
        //    $.get(url).done(function (data) {
        //        $('#wheel-edit-modal-placeholder').html(data);
        //        $('#wheel-edit-modal-placeholder > .modal').modal('show');
        //    });
        //});

        //$('a[data-toggle="repairtype-edit-modal"]').click(function (event) {
        //    var url = "/Repair/EditRepairType/" + $(this).attr('item');
        //    $.get(url).done(function (data) {
        //        $('#repairtype-edit-modal-placeholder').html(data);
        //        $('#repairtype-edit-modal-placeholder > .modal').modal('show');
        //    });
        //});

        //$('a[data-toggle="report-details-modal"]').click(function (event) {
        //    var url = "/Report/Details/" + $(this).attr('item');
        //    $.get(url).done(function (data) {
        //        $('#report-details-modal-placeholder').html(data);
        //        $('#report-details-modal-placeholder > .modal').modal('show');
        //    });
        //});

        //$('a[data-toggle="report-edit-modal"]').click(function (event) {
        //    var url = "/Report/Edit/" + $(this).attr('item');
        //    $.get(url).done(function (data) {
        //        $('#report-edit-modal-placeholder').html(data);
        //        $('#report-edit-modal-placeholder > .modal').modal('show');
        //    });
        //});

        //$('a[data-toggle="repair-details-modal"]').click(function (event) {
        //    var url = "/Repair/Details/" + $(this).attr('item');
        //    $.get(url).done(function (data) {
        //        $('#repair-details-modal-placeholder').html(data);
        //        $('#repair-details-modal-placeholder > .modal').modal('show');
        //    });
        //});

        //$('a[data-toggle="repair-edit-modal"]').click(function (event) {
        //    var url = "/Repair/Edit/" + $(this).attr('item');
        //    $.get(url).done(function (data) {
        //        $('#repair-edit-modal-placeholder').html(data);
        //        $('#repair-edit-modal-placeholder > .modal').modal('show');
        //    });
        //});

        $('a[data-toggle="ajax-modal"]').click(function (event) {
            var url = $(this).attr('item-url');
            $.get(url).done(function (data) {
                $('.modal').modal('hide');
                $('#ajax-modal-placeholder').html(data);
                $('#ajax-modal-placeholder > .modal').modal('show');
            });
        });
    });
</script>